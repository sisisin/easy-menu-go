main menu:
  - list files: 'ls -al --color=always'
  - echo command with option:
      env:
        ENV1: val_env1_override
      work_dir: ./tmp/
      run: |
        echo 1
        pwd
  - simple echo: 'echo simple!'
  - y: |
      ls -al --color=always
  - failed operation: 'cho simple!'
  - deploy menu:
      - server1:
          run: 'echo !! deploy server1 !!'
      - server2:
          run: 'echo !! deploy server2 !!'
  - eval menu:
      eval: |
        echo [{"eval list1": "echo list1!"}, {"eval list2", {"run": "echo list2!"}}]
env:
  ENV1: val_global_env
work_dir: ./tmp/work1
